# desc_layer 生产参数
# 用法: ros2 launch desc_layer production.launch.py

desc_layer:
  ros__parameters:
    http_port: 5000
    exec_action_name: "exec_task"
    maps_dir: "config/maps"
    db_dir: "config"
    api_token: ""  # 生产环境请通过 launch argument 传入
