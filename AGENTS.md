# AGENTS.md

本仓库的修改约定。所有改动都必须遵守以下规则。

## 1. 只用 `pub`，不用特殊 `pub`

Rust 可见性只允许两种写法：

- `pub`（对外可见）
- 不加任何修饰（模块内私有）

禁止使用任何特殊 `pub`：

- ❌ `pub(crate)`
- ❌ `pub(super)`
- ❌ `pub(self)`
- ❌ `pub(in path)`

```rust
// ❌ 错误
pub(crate) fn build_plan() {}

// ✅ 正确
pub fn build_plan() {}
// 或者
fn build_plan() {}
```

需要跨模块共享时，用 `pub`，并通过模块边界和重新导出来组织，而不是收紧可见性关键字。

## 2. 修订模块时移除陈旧逻辑

每次修改某个模块，都要把被这次修改淘汰的旧代码清掉，包括：

- 死代码、废弃分支
- 不再被引用的函数、字段、类型、导入
- 已经失效的配置、常量、注释

不要为“以防万一”保留旧实现。修订完成后，该模块只反映当前设计。

## 3. 注意 Rust 文件拆分

- 文件和模块按职责拆分，保持单一职责。
- 不要把多个不相关的关注点堆进同一个文件。
- 一个文件大到难以整体理解时，拆成子模块（目录 + 子文件），而不是继续追加。

## 4. 不写 giant function

- 禁止超大函数。
- 一个函数只做一件事，逻辑变长就拆分为职责清晰的小函数。
- 用明确的命名和小函数替代深层嵌套的大块逻辑。

## 5. replacement > parallel implementation

- 改变行为时，直接替换旧实现，不要新旧两套并存。
- 不要为了过渡保留双路径、开关或并行的第二套实现。
- 一次只留一套权威实现。

## 6. 实现不了 spec/design 时停止

- 如果按 spec/design 无法实现，立即停止，并说明冲突点、缺失信息或阻塞原因。
- 不要擅自偏离设计、猜测需求，或降级实现来“凑”过去。

## 7. 不写测试

- 不新增测试代码：单元测试、集成测试、测试脚手架一律不加。
- 验证依赖现有的检查/构建命令与手工验证，而不是新增测试。

## 8. 不添加兼容层、mapper、adapter

- 不新增兼容用途的中间层。
- 不为了兼容旧接口/旧调用方而添加 wrapper、mapper、adapter、bridge 之类的转换层。
- 直接修改目标代码，让接口本身正确，而不是在中间做转换。

> 注意：这条针对的是**兼容性**的中间层，不是仓库既有的业务 adapter（如 endpoint adapter）。
