#!/usr/bin/env bash
set -euo pipefail

echo "ROS transport packages are built from interfaces/ros when an endpoint profile enables them."
echo "The device-neutral control plane is built with: cargo build --workspace"
