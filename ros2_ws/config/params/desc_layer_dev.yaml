# desc_layer 开发参数（使用 mock_exec_layer）
# 用法: ros2 launch desc_layer mock.launch.py

desc_layer:
  ros__parameters:
    http_port: 5000
    exec_action_name: "mock_exec_task"
    maps_dir: "config/maps"
    db_dir: "config"
    api_token: ""
