# Hold current position / posture (or stand still) for a duration.
# Goal
string goal_id
int64 duration_ms   # 0 = indefinite until canceled
---
# Result
string final_state
string error_code
string message
---
# Feedback
string state
float32 progress
string error_code
string message
