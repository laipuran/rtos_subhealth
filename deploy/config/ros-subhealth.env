# Shared node environment. Prefer /usr/bin/ros-subhealth-run, which sources the
# ROS and workspace setup scripts; this file only carries values systemd needs
# before ExecStart.
RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
ROS_DOMAIN_ID=1
